import os
import cv2

image = cv2.imread()

# image_folder = os.path.join(os.getcwd(), 'E:20210521-0618_RAW_From-Server-Data')
# image_files = [_ for _ in os.listdir(image_folder) if _.endswith('jpg')]

image_list=[]
image_namelist=[]
for file_org in image_files:
    img = c2.imread()
    if img not in image_list:
        image_list.append(pix_mean1)
        #image_namelist.append(file_org)
        #이미지 저장dd
print(image_namelist)

폴더를 받고
파일을 리스트에
파일의 정보가 분류기 리스트에 없으면 폴더를 만들고 파일을 삽입
                     있으면 패스

image_folder =
image_files = 

for compare in image_files:
    img = compare
